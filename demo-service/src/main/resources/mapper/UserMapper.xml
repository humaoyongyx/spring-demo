<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="issac.study.springdemo.service.mapper.UserMapper">

     <insert id="save" parameterType="issac.study.springdemo.service.model.UserBean">
          insert into user(id,name) values (#{id},#{name})
     </insert>

</mapper>